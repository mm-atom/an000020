	// 生成二维码
	/**
	 * ${0:变量注释}
	 */
	const r$CURRENT_SECONDS_UNIX = await(() => {
		const p1 = $1;	// 生成二维码的文本
		const p2 = ${2:80};	// 二维码的宽度
		const p3 = '${3:#000000}';	// 二维码的颜色
		const p4 = '${4:#ffffff}';	// 二维码的背景色
		return an20(p1， p2， p3, p4);
	})();